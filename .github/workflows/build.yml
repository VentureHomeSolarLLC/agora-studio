name: Build and Deploy

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Validate YAML frontmatter
        run: |
          # TODO: Add YAML validation script
          echo "Validating YAML frontmatter..."
      
      - name: Check file naming
        run: |
          # TODO: Add naming convention checks
          echo "Checking file naming conventions..."

  build-customer-site:
    runs-on: ubuntu-latest
    needs: validate
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install dependencies
        run: cd scripts && npm install
      
      - name: Build customer site
        run: node scripts/build-customer-site.js
      
      - name: Deploy to GCP
        run: |
          # TODO: Add GCP deployment
          echo "Deploying to help.venturehome.com..."

  sync-to-forge:
    runs-on: ubuntu-latest
    needs: validate
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      
      - name: Sync Engrams to Forge
        run: |
          # TODO: Add Forge sync script
          echo "Syncing Engrams to Forge skills library..."
        env:
          FORGE_API_KEY: ${{ secrets.FORGE_API_KEY }}
